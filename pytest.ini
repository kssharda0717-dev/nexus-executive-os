[pytest]
# test_agents.py is a live end-to-end script that calls Gemini
# (run it with `python test_agents.py`). CI runs the mocked suite only.
addopts = -q --ignore=test_agents.py
asyncio_mode = strict
